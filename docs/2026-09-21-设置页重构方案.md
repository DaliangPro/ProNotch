# ProNotch 设置页重构方案（最终版）

- 日期：2026-09-21（同日经三轮迭代：v1 诊断七页 → v2 产品审阅十一条 → v3/v4 精简紧凑 → 最终版）
- 状态：**已实现**（2026-09-22，四笔提交：1ee9e15 主题/权限/冲突检测 → 45075c3 完成提醒勾选独立持久化 → 24c2d24 ChatStore 搜索单独保存与草稿回退 → 六页重建）；已装到 /Applications，离屏六图与样式稿逐页对照通过，764 项测试全绿；待大梁老师实机点验后升版本号发版
- 样式稿：`docs/design/2026-09-21-设置页重构.pen`（六张屏 + 两处弹层/状态；旧版本已全部删除）
- 前置：2.0 七区重构（`docs/2026-07-19-设置页重构执行方案.md`）已发版；此后 2.1～2.9 陆续塞行，分区没再整理
- 依据：通读 `Sources/ProNotch/Settings/`（SettingsView 1708 行）＋ `-snapshotSettings` 离屏渲染现状七页对照

## 一、诊断（简）

1. **同一件事拆在两页，靠说明文字互相指路**：时区在功能组件页、时间槽位在通用页；天气卡在功能组件页、天气槽位在通用页。全文件 21 段灰色说明文字，多数在解释「这个在另一页」。
2. **Agent 页四家出现三遍**：监控开关、完成提醒勾选、颜色行各一遍；`glowEnabled` 总开关重开会把所有家的钩子全装回，按家取舍一次关开就丢。
3. **两个 API 编辑器长得像用法不同**：闪问要点「保存」，翻译即时生效；「名称」一个在卡内一个单独成卡；「检测」对「测试」；闪问「模型」输入框与「模型列表」两处都能定当前模型。
4. **命名与内容不符**：「快捷剪切板」页内全写「剪贴板」，一半是话术；「超级截图」九成是翻译配置；「音量与亮度」不是组件卡却在功能组件页。
5. **视觉词汇太多**：粉色开关、青色胶囊、灰底按钮、红底按钮、下拉框、状态圆点、勾选框、色块同页并存，卡片描边加分割线，几乎每行一句副标题。
6. **窗口 660×540 不可缩放**，超级截图页实高约两个窗口；侧栏无图标。
7. **权限不可见**：辅助功能、定位、屏幕录制三处要么一句灰字要么没有；屏幕录制是截图最常见故障点。

顺带发现（另开任务）：两侧功能区只有 Claude Code / Codex 槽位，Grok / Kimi 没有（`NotchSlot` 写死）；时钟城市网格离屏快照空白疑为 LazyVGrid 离屏不绘制。快捷键无冲突检测——并入本轮。

## 二、沿用不动的既定决策

1. 每家菜单栏露出开关放在菜单栏面板各家详情页底部，不进设置页。
2. 快捷键跟功能走。
3. 本地 Agent 只列扫描发现的家；已勾选但已卸载的保留关闭入口。
4. 剪贴板「关＝停记录保留历史」，清空独立。
5. 面板页面随内容自动显隐（组件页三卡全关已会隐藏，核实过）。
6. ~~翻译与闪问接口清单各存各的~~ → 2026-09-22 大梁老师改口：两边共用一个接口池，一边配了另一边直接选。翻译的多套存档首启并入闪问接口池（ChatStore.mergeTranslateEndpoints，Key 沿用原钥匙串账号不搬），翻译只记「跟闪问」或池里某套的 id。翻译「共用闪问接口」默认开（核实过）。

## 三、设计语言（最终）

**五条规则**

1. 选择控件只留一种：单选用分段控件，多选用选片，长列表用下拉；胶囊与下拉两套颜色废除。**点亮即在用**：选片、磁贴都靠亮暗表示状态，不加对勾、不加外框。
2. 副标题只留会改变决定的（如「关闭后停止记录，历史保留」）；卡片下方独立说明段落全删。
3. 按钮只用文字：白色中等字重为操作（重新扫描、编辑、自定义、去授权），红色文字为破坏性（清空…、删除这套接口）；实心按钮只在弹层的「完成」。
4. 卡片无描边，无状态圆点：状态直接写「已授权」「未授权 · 去授权」。
5. 深层内容折进二级或条件显示：城市网格→一行摘要＋「编辑」；天气预览→「预览 ▾」；颜色→四个小圆点＋「自定义」；光晕外观三行滑块只在选「四周光晕」时出现；时区只在选「时间」时出现；权限警告只在未授权时出现。

**四种非清单表达**

| 表达 | 用在 | 替代了什么 |
|---|---|---|
| 刘海示意图：一条黑色屏幕带，中间刘海，两侧槽位就是下拉 | 刘海页顶部 | 两行各六格的槽位选择 |
| 磁贴：并列的同类项做成一排方块，只有图标/品牌点＋名字，点亮即启用，不嵌卡片、不加对勾、不写活跃天数 | 刘海页组件区（三张卡）、Agent 页本地 Agent（四家一排） | 三行 / 四行开关 |
| 服务商列表＋弹层：服务商只在 AI 模型配置页列成行，点「编辑」进弹层；功能页各自一个模型下拉 | AI 模型配置页 | 页内平铺的编辑卡、两页各一份的接口列表（2026-09-22 被否：「太丑」，根子是用户没有「接口」概念） |
| 总开关＋二选一分段＋条件行：开关决定要不要，分段决定哪种，只展开选中那种的细节 | Agent 完成提醒 | 光晕与弹窗设置同页并列 |

**文案**：行标签用最短的名词（快捷键 / 引擎 / 接口 / 时区 / 方式 / 哪些家），页面标题和小节标题已给上下文，不重复；副标题只剩三处（记录剪贴板、哪些家、权限对应功能）。

**配色**（2026-09-22 最终）：纯中性灰黑，R = G = B，不带色相；强调色跟随系统。

| 用途 | 色值 |
|---|---|
| 侧栏 | #1F1F1F |
| 窗口底 | #262626 |
| 卡片 / 弹层 | #303030 |
| 分割线 | #3C3C3C |
| 最深一档 | #141414 |
| 正文 / 操作文字 | #FAFAFA |
| 次级文字 | #C0C0C0 |
| 说明文字 | #9A9A9A |
| 强调（开关、滑块、弹层主按钮、录快捷键） | 系统强调色 controlAccentColor |
| 破坏性 / 错误 | #DD5353 |
| 连接正常 | #65BB30 |

走过的弯路：冷黑灰偏蓝 → 取 Claude 桌面端暖灰（色相 60 度）发绿 → 色相挪到 30 度又偏红 → 大梁老师定「正经的灰黑色」，灰阶一律不带色相，明度沿用暖灰那版的层级。强调色曾自定义成陶土橙，同日改回跟随系统（系统设置 → 外观 → 强调色）。磁贴、选片、分段控件用白色百分比叠底，本身就是中性的。

**规格**：窗口 700 宽、默认 620 高、可缩放记住尺寸；说明文字 12pt、白 60%；行高 44，卡片圆角 10；侧栏 176 宽带 SF Symbol 图标。

## 四、六页结构（最终）

侧栏顺序（2026-09-22 定稿，无图标）：通用 → 刘海设置 → AI 闪问 → 超级截图 → 剪贴板与话术 → Agent 提醒 → AI 模型配置（大梁老师定：放最后）。

### 通用（`gearshape`）
- 开机自动启动
- 软件：ProNotch 版本 · 软件更新（已是最新 / 检查更新）· 项目主页
- 权限：辅助功能（音量与亮度提示）· 定位（天气）· 屏幕录制（超级截图）；已授权写字，未授权红字＋「去授权」

### 刘海设置（`macbook`）
- 刘海卡：真实预览（假菜单栏 + 黑色胶囊 + 两侧按真实样式渲染内存环 / 天气 / 时钟；2026-09-22 点验后由抽象示意图改成这个）· 左侧 / 右侧 [关闭 | 内存 | 天气 | 时间]（Claude Code / Codex 精灵槽位同日去掉）· 时区（条件）· 全屏时隐藏 · 显示在 [全部屏幕 | 仅主屏 | 仅副屏]
- 组件页：内存卡 / 世界时钟 / 天气卡三张磁贴（独立一排，不嵌卡）· 下方一张卡：世界时钟城市（摘要＋编辑，条件）· 天气预警（开关＋「预览 ▾」）· 预警类型选片（条件）
- 音量与亮度：按键时在刘海显示 [音量] [亮度]

### 超级截图（`camera.viewfinder`）
- 截图：快捷键 · 权限警告行（仅未授权时）
- 翻译：引擎 [系统翻译 | AI 模型] · 翻译成 ▾ · 深度思考 · 并行加速 · 提示词（默认 / 编辑）
- 模型 ▾（仅 AI 模型时；ModelPicker，顶上多「跟随闪问」，就在本页选）
- 选「系统翻译」时翻译接口行换成「语言包 · 下载…」

### 剪贴板与话术（`doc.on.clipboard`）
- 剪贴板：记录剪贴板（副标题：关闭后停止记录，历史保留）· 保留条数 ▾ · 快捷键 · 历史记录 → 清空…（红字）
- 常用话术（标题右「新增」）：一行一条，标题＋预览，悬停出编辑/删除

### AI 闪问（`bubble.left.and.text.bubble.right`）
- 快捷键
- 对话：模型 ▾（ModelPicker，按服务商分组，就在本页选）· 深度思考
- 联网搜索：引擎 [DuckDuckGo | Tavily | Brave | 博查] · Key 行（条件，右侧「测试」）
- 页上没有「保存」按钮

### Agent 提醒（`cpu`）
- 本地 Agent（标题右「重新扫描」）：四家一排磁贴，只有品牌点＋名字，点亮即监控，不嵌卡、不加对勾、不写活跃天数（大梁老师 2026-09-21 定）；下一行灰字列其他已发现工具；未点亮的家不出现在「哪些家」选片里
- 完成提醒：完成时提醒（开关）· 方式 [四周光晕 | 顶部弹窗] · 呼吸周期 / 强度 / 厚度三行滑块（仅光晕）· 哪些家（选片，副标题「会写入该 Agent 的钩子配置」；失败行内红字）· 颜色（四点＋自定义）
- 额度：菜单栏显示额度

### AI 模型配置（最后一页，2026-09-22 新增）
- 只管服务商：分区头「服务商」右侧文字「新增」；卡片里一行一家——名字＋副标题「域名 · N 个模型」＋文字「编辑」，点开 AccountSheet 编辑名称 / 地址 / Key / 模型列表 / 测试 / 删除。服务商只是池子，没有「当前」；不放任何功能的选项——同一件事不拆在两页互相指路（第一版把「用哪个模型」放这里、功能页只留摘要+跳转，被大梁老师指出是大问题）
- 叫「服务商」不叫「账号」：每条是一家的地址＋Key＋模型列表，不是登录账号（2026-09-22 第二次点验改）；不用磁贴：磁贴是「点亮即在用」的开关语言，服务商没有开关态，大方块配首字母徽记只占地方（同次点验否掉）
- 加完服务商若闪问还没配过，直接用上它

### 服务商编辑弹层（AccountSheet）
名称 · API 地址 · API Key · 模型列表（标题带计数；获取模型 / 手动添加 / 手动加的可移除；**列表限高 200pt 内滚动、添加行钉在底下**——百炼 261 个模型曾把弹层撑到屏幕外，2026-09-22 大梁老师点验修）· 测试连接（没测过不显示状态行）· 删除服务商 · 取消 / 完成。字段是草稿，「完成」才写回。离屏核对：`-snapshotSettings -sheetModels 261` 出 `/tmp/pronotch-sheet-261.png`。

## 五、交互规范

- **完成提醒**：删 `glowEnabled` 语义中的「重开全装回」。新增 `alertAgents`（勾选集，持久化）；钩子安装条件 = 任务完成时提醒开 ∧ 该家 ∈ enabledAgents ∧ 该家 ∈ alertAgents。总开关关只移除钩子，勾选集保留。刘海面板「Agent 提醒」按钮就是这个总开关。老用户迁移：`alertAgents` 首启取当前已装钩子的家。
- **钩子写入**：副标题写明会写该 Agent 配置；`setInstalled` 失败勾选保持原状、行内红字给原因。
- **监控关掉**：该家从「提醒哪些家」选片里消失（不是灰显）。
- **接口即时生效**：弹层「完成」即写入；页上模型下拉、深度思考切换即生效；ChatStore 的 draft→save 路径改直写。
- **翻译接口下拉**：选项 = 「同闪问 · <闪问当前接口名>」＋ 翻译专用各套 ＋「新建…」；选翻译专用套时行右多一个「编辑」。
- **条件行**：时区、城市、预警类型、Key 行、权限警告、光晕滑块均按状态显隐，不灰显。
- **权限**：通用页集中三项（AXIsProcessTrusted / CLLocationManager.authorizationStatus / CGPreflightScreenCaptureAccess）；功能页只在未授权时出警告行。
- **快捷键冲突**：录制时与别处相同则录入框下红字「已被超级截图使用」，不保存。
- **命名**：「剪切板」统一「剪贴板」（含面板顶部标签）；「系统提示」不用。未上线功能不出现占位行。

## 六、代码拆分（零行为变更）

```
Settings/
  SettingsView.swift              壳：侧栏＋路由＋pendingSection
  SettingsSection.swift           六项枚举＋图标
  Components/SettingsRows.swift   Card / Row / Switch / Segmented / Chips / Popup / TextButton / ShortcutField / Slider
  Components/NotchDiagram.swift   刘海示意图
  Components/WidgetTiles.swift    组件磁贴
  Components/EndpointSheet.swift  接口编辑弹层（闪问、翻译共用）
  Pages/GeneralPage.swift  NotchPage.swift  ScreenshotPage.swift  ClipboardPage.swift  ChatPage.swift  AgentPage.swift
```

`-snapshotSettings -section` 与 `pendingSection` 三处入口继续走枚举（chat→AI 闪问、glow→Agent）。

## 七、实施步骤（原子提交，每步 build＋test＋离屏图核对）

实际执行时把 S1（纯拆文件）与 S2～S6 合并：六页内容全部重写，先做零行为变更的搬迁是纯粹的抛弃性工作。改为四笔提交：基础件（主题 / 权限 / 冲突检测）→ 数据层（alertAgents）→ ChatStore 小改 → 六页整体重建。两处与样式稿的有意偏差：翻译页多一行「深度思考」（translateThinking 是全局标志，不按接口存，放弹层里会丢）；接口弹层里翻译那套没有「取消」（字段即时生效，无草稿可退）。

| 步骤 | 内容 | 类型 |
|---|---|---|
| S1 | 纯拆文件：壳＋组件＋六页，零行为变更，离屏图拆前拆后一致 | 重构 |
| S2 | 设计语言落地：Row/Segmented/Chips/Popup/TextButton 组件，卡片去描边，副标题清理，说明段落删除，窗口可缩放＋侧栏图标 | 重构 |
| S3 | 六页 IA 搬迁＋命名统一（关于并入通用、面板组件并入刘海、权限卡集中、条件行） | 重构 |
| S4 | 刘海示意图＋组件磁贴＋Agent 磁贴＋城市编辑弹层 | 新增 |
| S5 | 接口列表＋共用编辑弹层；闪问去「保存」、模型列表唯一入口；翻译接口改下拉（ChatStore 直写，配单测） | 优化 |
| S6 | Agent：总开关＋二选一＋条件滑块、`alertAgents` 勾选集与迁移、钩子失败反馈、监控关掉即从选片消失 | 重构＋修复 |
| S7 | 权限三项 API＋警告行、快捷键冲突检测 | 新增 |
| S8 | 点验＋发版（升次版本号；发版说明附「搬去哪了」对照表；本次之后分区冻结） | 配置 |

S5、S6 碰数据层，各自独立成笔，可单独回退。

## 八、拍板记录

- v1（七页）→ 产品审阅 11 条，大梁老师拍板全收含两页合一 → v2。
- v2 仍觉杂乱 → v3 五条精简规则（控件、副标题、文字按钮、去描边、折叠），刘海页控件 48→17。
- v3 刘海页仍长 → v4 四种非清单表达（示意图、磁贴、权限集中、条件行），刘海页 946→673，大梁老师认可「合理很多」。
- 2026-09-21 要求出最终版并删除旧稿；追加方向：同层级选项（光晕/弹窗）不必同页并列，用开关＋二选一切换。最终版据此把六页全部按 v4 语言重画。
- 配色几轮：去蓝→压深→取 Claude 桌面端深色主题变量（暖灰＋陶土橙）→ 发绿挪色相 → 又偏红 → 2026-09-22 最终定纯中性灰（R = G = B），强调色跟随系统。
- 2026-09-22 AI 接口重想：两页各摆一份接口列表被否，改为「账号池＋按功能选模型」，新增 AI 模型配置页；分区改名（刘海设置 / AI 模型配置 / Agent 提醒）、去图标、重排。再审：模型选择从 AI 模型配置页挪回各功能页（跨页指路是大问题），AI 模型配置页只管账号且放最后。再点验：「账号」改叫「服务商」，磁贴改成列表行。收尾文案整体精简一轮：空态不复述、警告写结果不写机制、弹层状态行没测过不显示。
- 2026-09-22 实机点验追加：刘海示意图太抽象→改真实预览（再去掉假菜单栏背景）；刘海里两家 Agent 运行状态显示去掉；所有开关加滑动动效；色相 60°→30° 去发绿；「时钟卡」改叫「世界时钟」且组件页时钟卡加日期；闪问与翻译接口合成一个池子。
- 最终版点验追加：Agent 页四家改成一排磁贴点亮，不用四行开关。再追加：磁贴去对勾、去外框、去活跃天数，全站文案压到最短名词，副标题只剩三处。六页高度：通用 528、刘海 632、截图 442、剪贴板 568、闪问 530、Agent 580，全部一屏内。
